#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isalpha - check for alphabetic
 * @c: character
 * Return: i if letter, 0 if not
int _isalpha(int c)
{
	return _isalpha(c);
}
